import styled from "@emotion/styled";

export const TooltipFloatingContentWrapper = styled.div`
  max-width: 259px;

  line-height: 18.2px;
  font-size: 14px;
  font-weight: 400;
  word-break: keep-all;

  b {
    font-weight: 700;
  }
  a {
    text-decoration: underline;
    text-underline-offset: 3px;
  }
`;
