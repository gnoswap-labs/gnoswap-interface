import React from "react";
import { wrapper } from "./Staking.styles";

interface StakingProps {}

const Staking: React.FC<StakingProps> = ({}) => {
  return <div css={wrapper}>Staking</div>;
};

export default Staking;
