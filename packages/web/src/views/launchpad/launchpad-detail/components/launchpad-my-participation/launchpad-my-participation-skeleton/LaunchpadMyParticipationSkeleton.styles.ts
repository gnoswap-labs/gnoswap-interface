import styled from "@emotion/styled";
import mixins from "@styles/mixins";

export const ParticipationSkeletonWrapper = styled.div`
  ${mixins.flexbox("row", "center", "center")};
  padding: 16px;
  width: 100%;
`;
