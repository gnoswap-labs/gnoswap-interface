import SwapWidget from "@components/home/swap-widget/SwapWidget";
import React from "react";

const SwapWidgetContainer: React.FC = () => {
  return <SwapWidget />;
};

export default SwapWidgetContainer;
