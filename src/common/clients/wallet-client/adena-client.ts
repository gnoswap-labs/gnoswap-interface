import { InjectResponse } from "./protocols";
import { WalletClient } from "./wallet-client";

export class AdenaClient {
	// export class AdenaClient implements WalletClient {
	// public existsWallet = (): boolean => {
	// 	if (window?.adena)
	// };
	// public addEstablishedSite = (sitename: string): Promise<InjectResponse<any>> => {
	// };
	// public sendTransaction = (transaction: Transaction): Promise<InjectResponse<any>> => {
	// };
	// public getAccount = (): Promise<InjectResponse<any>> => {
	// };
}
